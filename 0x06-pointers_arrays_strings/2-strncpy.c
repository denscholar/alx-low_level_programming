#include "main.h"

/**
 * _strncpy - Copies strings based on number
 * @dest: first string
 * @src: second string
 * @n: number of char to concat
 * Return: string
 */

char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
